#!/bin/sh
#shellcheck shell=ash
set -euo pipefail
. /usr/local/lib/scp

if [ $# -lt 1 ]; then
    scp_fatal "usage: ${0} <method> [args...]"
fi

#shellcheck disable=SC2169
_method="scp_${1/-/_}"; shift
${_method} "${@}"
